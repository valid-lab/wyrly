{
  "threshold": 0.05,
  "note": "Calibrated for GitHub Actions ubuntu-latest (not local dev). minHz = floor(measured_hz × 0.95) from first green bench-regression run 2026-05-30. Local/WSL is often 1.5–2.5× faster; use deno task bench:di locally. Raise minHz after intentional perf wins on CI.",
  "suites": {
    "resolution": { "minHz": 123338 },
    "cold_start": { "minHz": 106495 },
    "cold_start_resolution": { "minHz": 40660 },
    "request_scope": { "minHz": 237614 }
  }
}
